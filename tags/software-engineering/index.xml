<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>软件工程 on 向叔记事簿</title>
        <link>https://ttf248.life/tags/software-engineering/</link>
        <description>Recent content in 软件工程 on 向叔记事簿</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>zh-cn</language><atom:link href="https://ttf248.life/tags/software-engineering/index.xml" rel="self" type="application/rss+xml" /><item>
        <title>AI 写 Demo 很快，返工也是真的快</title>
        <link>https://ttf248.life/p/ai-demo-fast-rework-faster/</link>
        <pubDate>Fri, 10 Apr 2026 15:39:51 +0800</pubDate>
        
        <guid>https://ttf248.life/p/ai-demo-fast-rework-faster/</guid>
        <description>&lt;p&gt;最近拿 AI 起一个 C++ 小项目，最容易傻眼的时刻，不是它写不出来，而是它三分钟给你拼出一套像模像样的目录树，顺手再塞几个三方库，Demo 还真能跑。问题也在这。你连新引进来的库到底支持什么、编译链路怎么走、边界在哪，都还没摸清，后面返工基本跑不掉。&lt;/p&gt;
&lt;p&gt;我现在越来越觉得，AI 编程最怕的不是模型笨，而是起手太贪。尤其是 C++ 这种没什么脚手架兜底的语言，你前面少想一步，后面就得在编译、链接、库版本、目录结构上多还几步。&lt;/p&gt;
&lt;p&gt;这事我自己的结论很直接，AI 更适合当一个推进很快的结对程序员，不适合一上来就替你把前期设计和依赖决策全包了。GitHub 的文档建议一开始先给简单一点的任务，先让 agent 把 bug、文档、测试、技术债这种边界更清楚的活干明白；Anthropic 的 Plan Mode 文档则更像是给复杂改动上的保险，先读代码、先出计划，再决定改不改。两边说法不完全一样，但指向差不多，别一上来就把最难、最乱、依赖最多的那一坨全扔给 AI。&lt;/p&gt;
&lt;p&gt;所以这篇不聊一大堆规范，就给一个能直接练手的小案例。你拿任意一个空 C++ 仓库都能照着来。&lt;/p&gt;
&lt;h2 id=&#34;不是-ai-不行是起手太贪&#34;&gt;不是 AI 不行，是起手太贪
&lt;/h2&gt;&lt;p&gt;人工写项目，尤其是 C++，很多时候就是很土的办法。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;先从 &lt;code&gt;main.cpp&lt;/code&gt; 起一个最小可运行版本&lt;/li&gt;
&lt;li&gt;先把编译链路跑通&lt;/li&gt;
&lt;li&gt;再决定引什么库&lt;/li&gt;
&lt;li&gt;每加一步都保证还能编&lt;/li&gt;
&lt;li&gt;等业务轮廓出来了，再开始重构&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这套办法看起来慢，其实很稳。因为每一步你都知道自己到底换来了什么，也知道问题是从哪一步长出来的。&lt;/p&gt;
&lt;p&gt;AI 容易把这套节奏打乱。它很喜欢一步到位地“帮你完整一下”：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;配置文件顺手给你上了&lt;/li&gt;
&lt;li&gt;日志模块顺手给你抽了&lt;/li&gt;
&lt;li&gt;错误码、工具类、目录结构顺手一起整理了&lt;/li&gt;
&lt;li&gt;三方库的接入方式，它也替你选了&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;结果就是，Demo 看起来完整了，你的心智模型却是空的。后面只要有一个库的能力边界和你想的不一样，返工就不是一处，而是一串。&lt;/p&gt;
&lt;h2 id=&#34;先拿一个小项目练手&#34;&gt;先拿一个小项目练手
&lt;/h2&gt;&lt;p&gt;我觉得很适合练手的，是做一个很小的日志扫描工具，名字随便起，比如 &lt;code&gt;logscan&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;它只做一件事：扫一个目录下的日志文件，统计 &lt;code&gt;error&lt;/code&gt; 和 &lt;code&gt;warn&lt;/code&gt; 的数量，然后把结果打印出来。这个题目不大，但正好够你把 C++ 小项目里最常见的几块都走一遍：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;main&lt;/code&gt; 和参数入口&lt;/li&gt;
&lt;li&gt;输出格式化&lt;/li&gt;
&lt;li&gt;日志&lt;/li&gt;
&lt;li&gt;配置文件&lt;/li&gt;
&lt;li&gt;业务模块拆分&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;关键不是题目多高级，关键是拆法。&lt;/p&gt;
&lt;p&gt;我会把它拆成四步，而且每一步只允许 AI 做当前这一小步。&lt;/p&gt;
&lt;h2 id=&#34;第一步先把输出做好&#34;&gt;第一步，先把输出做好
&lt;/h2&gt;&lt;p&gt;第一步别碰日志，别碰配置，别想着抽象。&lt;/p&gt;
&lt;p&gt;就做两件事：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;建一个最小 &lt;code&gt;main.cpp&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;引入 &lt;code&gt;fmt&lt;/code&gt;，把扫描目录和统计结果打印出来&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;fmt&lt;/code&gt; 官方文档给了很清楚的 CMake 用法，它有 &lt;code&gt;fmt::fmt&lt;/code&gt; 和 &lt;code&gt;fmt::fmt-header-only&lt;/code&gt; 两个 target，文档还明确更推荐编译版，理由也很朴素，编译时间更友好。CMake 的 &lt;code&gt;FetchContent&lt;/code&gt; 文档也很适合新项目起步，因为它是在 configure 阶段把依赖拉进来，不是 build 阶段才临时下载。&lt;/p&gt;
&lt;p&gt;这一步我会直接把依赖策略定死，不让 AI 自由发挥。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;要么统一 &lt;code&gt;FetchContent&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;要么统一 &lt;code&gt;find_package&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;不要一会 vcpkg，一会 &lt;code&gt;add_subdirectory&lt;/code&gt;，一会再手拷头文件&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果是练手项目，我一般会这么约束 AI：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-text&#34;&gt;不要一次性优化架构。
当前目标只有这一步，而且项目必须始终可编译。

这一步只做 3 件事：
1. 在根 CMakeLists.txt 里引入 fmt
2. main.cpp 用 fmt 打印传入目录和扫描结果
3. 保持现有目标名和目录结构，不新增日志、配置、测试框架

先给我修改清单，再给代码。
完成后给出编译命令和预期输出。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里有个细节挺重要。AI 一旦没被约束，它很容易把“顺手把后面也做了”当成加分项。你得明确告诉它，这一轮不准越界。&lt;/p&gt;
&lt;h2 id=&#34;第二步再上日志&#34;&gt;第二步，再上日志
&lt;/h2&gt;&lt;p&gt;等第一步能编、能跑、输出也顺眼了，再加 &lt;code&gt;spdlog&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;spdlog&lt;/code&gt; 官方 README 其实已经把它能干什么写得很直白了。它既能做控制台日志，也能做普通文件、滚动文件、按天切分，甚至还有 backtrace ring buffer。问题就在这。能力一多，AI 就容易兴奋，恨不得第一版把彩色控制台、滚动日志、按天归档、全局 logger factory 一把梭。&lt;/p&gt;
&lt;p&gt;没必要。&lt;/p&gt;
&lt;p&gt;这一步我只会让它做控制台日志，或者最多加一个最简单的文件日志。因为你这时候真正要学的，不是“怎么把日志系统设计完整”，而是三件事：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;日志初始化放哪&lt;/li&gt;
&lt;li&gt;业务里怎么拿到 logger&lt;/li&gt;
&lt;li&gt;出错时日志和正常输出怎么分工&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果你一上来就上 &lt;code&gt;rotating_file_sink&lt;/code&gt;、&lt;code&gt;daily_file_sink&lt;/code&gt;，你虽然很快拥有了一套“功能很全”的日志模块，但你其实还不知道自己到底需不需要轮转、按天切分，还是只要调试阶段打印到终端就够了。&lt;/p&gt;
&lt;p&gt;说白了，先把 &lt;code&gt;spdlog::info()&lt;/code&gt; 用明白，比先把日志工厂画漂亮更重要。&lt;/p&gt;
&lt;h2 id=&#34;第三步才碰配置文件&#34;&gt;第三步，才碰配置文件
&lt;/h2&gt;&lt;p&gt;配置文件这块更容易被 AI 搞成大工程。&lt;/p&gt;
&lt;p&gt;我更偏向拿 &lt;code&gt;toml++&lt;/code&gt; 这种简单一点的库来练手，不是因为它“最强”，而是因为它很适合新项目前几步。它本身就是 C++17 的 header-only TOML parser，README 里给的例子也很短，&lt;code&gt;toml::parse_file(&amp;quot;configuration.toml&amp;quot;)&lt;/code&gt; 就能直接读。更关键的是，它单头文件模式真的很省事，官方原话都挺有意思，single-header 方案就是“把 &lt;code&gt;toml.hpp&lt;/code&gt; 扔进源码树里”，然后“没有第二步了”。&lt;/p&gt;
&lt;p&gt;这一步别做“配置中心”，就读三个字段：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;扫描目录&lt;/li&gt;
&lt;li&gt;关键字列表&lt;/li&gt;
&lt;li&gt;输出是否写文件&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然后把它们塞进一个很薄的 &lt;code&gt;AppConfig&lt;/code&gt; 结构体里。&lt;/p&gt;
&lt;p&gt;就够了。&lt;/p&gt;
&lt;p&gt;很多返工，其实就出在这里。你自己还没想清楚配置到底是启动时读一次，还是运行时热更新；是只给本地 CLI 用，还是以后还要给服务进程复用。AI 已经提前替你抽了五六层。后面你一改方向，之前那点“完整设计”就都成了包袱。&lt;/p&gt;
&lt;h2 id=&#34;第四步再拆业务模块&#34;&gt;第四步，再拆业务模块
&lt;/h2&gt;&lt;p&gt;等 &lt;code&gt;fmt&lt;/code&gt;、&lt;code&gt;spdlog&lt;/code&gt;、&lt;code&gt;toml++&lt;/code&gt; 都是你自己看过一遍官方说明，也都实际跑通过了，再让 AI 帮你拆模块。&lt;/p&gt;
&lt;p&gt;这时候拆，心里才有数。&lt;/p&gt;
&lt;p&gt;我一般会收成这么几个文件：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;main.cpp&lt;/code&gt; 只负责组装&lt;/li&gt;
&lt;li&gt;&lt;code&gt;app_config.{h,cpp}&lt;/code&gt; 只负责配置读取&lt;/li&gt;
&lt;li&gt;&lt;code&gt;logger.{h,cpp}&lt;/code&gt; 只负责日志初始化&lt;/li&gt;
&lt;li&gt;&lt;code&gt;log_scanner.{h,cpp}&lt;/code&gt; 放业务逻辑&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;注意，是到这一步才拆，不是第一步就拆。第一步就把目录排得整整齐齐，很多时候只是视觉上舒服，认知上并没有更清楚。&lt;/p&gt;
&lt;h2 id=&#34;真正有用的不是什么复杂规范&#34;&gt;真正有用的，不是什么复杂规范
&lt;/h2&gt;&lt;p&gt;网上很多 AI 编程规范写得很复杂，十几条、几十条，看着就累。我自己现在留下来的，其实就三条，够用了。&lt;/p&gt;
&lt;p&gt;第一条，先做一张“引库卡”。&lt;/p&gt;
&lt;p&gt;每次准备引三方库，我先让 AI 只回答这四个问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这个库解决什么问题&lt;/li&gt;
&lt;li&gt;官方已经支持到哪&lt;/li&gt;
&lt;li&gt;我这次只准备先用哪 1 到 2 个能力&lt;/li&gt;
&lt;li&gt;它会给构建、部署、运行带来什么新约束&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果这张卡说不明白，就先别接。&lt;/p&gt;
&lt;p&gt;第二条，一次只让 AI 过一个检查点。&lt;/p&gt;
&lt;p&gt;比如这一轮只允许它完成：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;能编过&lt;/li&gt;
&lt;li&gt;能运行&lt;/li&gt;
&lt;li&gt;能看到预期输出&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;别在同一轮里再加“顺手重构一下目录”“顺手补个配置类”“顺手把异常体系也统一了”。C++ 项目里，越是“顺手”，越容易把问题来源弄脏。&lt;/p&gt;
&lt;p&gt;第三条，每轮都要留一个回滚点。&lt;/p&gt;
&lt;p&gt;哪怕你不 commit，也得能回答：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这一步到底改了什么&lt;/li&gt;
&lt;li&gt;如果错了，要删哪几处&lt;/li&gt;
&lt;li&gt;退回上一步以后，项目是不是还能编&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;你会发现，这套思路其实跟人工开发很像。区别只是以前你自己敲代码，现在是你盯着 AI 不让它乱跑。&lt;/p&gt;
&lt;h2 id=&#34;一个更像实战的-ai-用法&#34;&gt;一个更像实战的 AI 用法
&lt;/h2&gt;&lt;p&gt;如果你真想把这套东西练熟，我建议你连续做三轮。&lt;/p&gt;
&lt;p&gt;第一轮，只让 AI 帮你接 &lt;code&gt;fmt&lt;/code&gt;，你自己读一遍 CMake 配置和输出代码。&lt;/p&gt;
&lt;p&gt;第二轮，只让 AI 接 &lt;code&gt;spdlog&lt;/code&gt;，你自己决定到底是控制台日志还是文件日志，不准两种一起上。&lt;/p&gt;
&lt;p&gt;第三轮，只让 AI 接 &lt;code&gt;toml++&lt;/code&gt;，你自己定配置项，不准 AI 帮你脑补未来需求。&lt;/p&gt;
&lt;p&gt;这三轮走完，你对 AI 编程的感觉会变很多。以前是“它怎么一下子给我生成这么多”，后来会慢慢变成“我该把下一步压到多小，才最划算”。&lt;/p&gt;
&lt;p&gt;我觉得这才是现在 AI 编程里最值钱的能力。&lt;/p&gt;
&lt;p&gt;不是把一个 Demo 变出来。&lt;/p&gt;
&lt;p&gt;而是把一个项目稳稳地往前推。&lt;/p&gt;
&lt;p&gt;说实话，这套办法没什么新鲜的，甚至有点土。但是土办法往往抗返工。尤其 C++ 这种语言，你前面让 AI 少做一点，自己多确认一点，后面真的会轻松很多。不然三分钟起飞，三小时回炉，傻眼的还是自己。&lt;/p&gt;
&lt;h2 id=&#34;参考资料&#34;&gt;参考资料
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://docs.anthropic.com/en/docs/claude-code/common-workflows&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Anthropic, Claude Code Common Workflows&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://docs.anthropic.com/en/docs/claude-code/overview&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Anthropic, Claude Code Overview&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://docs.github.com/en/copilot/tutorials/cloud-agent/get-the-best-results&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;GitHub Docs, Best practices for using GitHub Copilot to work on tasks&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://cmake.org/cmake/help/latest/module/FetchContent.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;CMake Docs, FetchContent&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://fmt.dev/latest/get-started/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;{fmt} Get Started&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/gabime/spdlog&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;gabime/spdlog README&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/gabime/spdlog/wiki/Sinks&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;gabime/spdlog Wiki: Sinks&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/marzer/tomlplusplus&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;marzer/tomlplusplus README&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;写作附记&#34;&gt;写作附记
&lt;/h2&gt;&lt;h3 id=&#34;原始提示词&#34;&gt;原始提示词
&lt;/h3&gt;&lt;blockquote&gt;
&lt;p&gt;提示词：人工写项目，针对 C++ 这种不用手脚架的语言，第一个弄个 框架，从 main 开始拓展逻辑，逐步增加模块，比如配置文件、日志模块、迭代业务模块。我的习惯是每次都能确保编译，逐步重构，可能一开始不是最优解。AI 编程，现在很多时候，跳过了步骤，前期思考的太少，特别是引入新的三方库，AI 是能快速出个 Demo，但是由于我不清楚新引入三方库的细节，支持哪些功能，相关逻辑的返工率明显高于人工开发。有没有好的实战案例，让我学下，我知道网上有很多复杂的 AI 编程规范，我想要简单一些的，能快速落地的。&lt;/p&gt;&lt;/blockquote&gt;
&lt;h3 id=&#34;写作思路摘要&#34;&gt;写作思路摘要
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;把题目收窄到 &lt;code&gt;tooling&lt;/code&gt; lane，重点写 AI 改变工作流以后，哪里省事了，哪里反而更别扭。&lt;/li&gt;
&lt;li&gt;不写成泛泛的规范清单，改成一个 C++ 小项目的递进式练手案例。&lt;/li&gt;
&lt;li&gt;保留用户“从 &lt;code&gt;main&lt;/code&gt; 开始、每一步都能编译、逐步重构”的核心判断，把它写成全文主线。&lt;/li&gt;
&lt;li&gt;补了 Anthropic、GitHub、CMake、fmt、spdlog、toml++ 的一手资料，避免对工具能力和依赖方式瞎猜。&lt;/li&gt;
&lt;li&gt;文章结构按 &lt;code&gt;fmt -&amp;gt; spdlog -&amp;gt; toml++ -&amp;gt; 模块拆分&lt;/code&gt; 推进，故意不让 AI 一步到位。&lt;/li&gt;
&lt;/ul&gt;</description>
        </item>
        <item>
        <title>文件夹分层再套 namespace，这事到底叫什么</title>
        <link>https://ttf248.life/p/cpp-folder-namespace-what-is-it-called/</link>
        <pubDate>Thu, 09 Apr 2026 02:31:07 +0800</pubDate>
        
        <guid>https://ttf248.life/p/cpp-folder-namespace-what-is-it-called/</guid>
        <description>&lt;p&gt;最近写算法服务，&lt;code&gt;twap&lt;/code&gt;、&lt;code&gt;vwap&lt;/code&gt; 这类模块一铺开，我又把这个老问题翻出来了。&lt;/p&gt;
&lt;p&gt;C++ 里如果还靠类名硬扛语义，名字很快就会失控。&lt;code&gt;TwapOrderManager&lt;/code&gt;、&lt;code&gt;VwapOrderManager&lt;/code&gt;、&lt;code&gt;AlgoOrderManager&lt;/code&gt; 这种东西，写着写着就一股子“我知道自己结构没收住，但我先把前缀补上”的味道。说白了，按文件夹分层，再配一层 &lt;code&gt;namespace&lt;/code&gt;，不是代码洁癖，这是在补 C++ 没有 Java 那种原生 &lt;code&gt;package&lt;/code&gt; 体系的空位。&lt;/p&gt;
&lt;h2 id=&#34;先说结论&#34;&gt;先说结论
&lt;/h2&gt;&lt;p&gt;如果非要找一个最贴近的软件工程名词，我觉得最该说的是两个词：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;模块化&lt;/strong&gt;（modularization）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;命名空间管理&lt;/strong&gt;（namespace management）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果再说得更贴一点，这类目录组织往往是在做：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;按特性分包 / 按领域分包&lt;/strong&gt;，也就是常说的 &lt;code&gt;package by feature&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;如果业务边界已经很强，还会带一点 &lt;strong&gt;bounded context&lt;/strong&gt; 的味道&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，它不是一个只有单一标准译名的小概念，更像是几套设计原则叠在一起。&lt;/p&gt;
&lt;h2 id=&#34;这事先解决的其实不是好看&#34;&gt;这事先解决的，其实不是“好看”
&lt;/h2&gt;&lt;p&gt;很多人一看到 &lt;code&gt;namespace&lt;/code&gt;，第一反应是避免重名。这个当然对，C++ 标准里的 &lt;code&gt;namespace&lt;/code&gt; 本来就是拿来防止大型项目里名字冲突的 &lt;a class=&#34;link&#34; href=&#34;https://en.cppreference.com/w/cpp/language/namespace&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;cppreference&lt;/a&gt;。但真到业务代码里，它的价值远不止这个。&lt;/p&gt;
&lt;p&gt;目录和 &lt;code&gt;namespace&lt;/code&gt; 一旦对齐，类名就不用再把上层语义重复一遍了。&lt;/p&gt;
&lt;p&gt;比如以前可能会写成这样：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-cpp&#34;&gt;class TwapOrderManager;
class TwapScheduleEngine;
class VwapOrderManager;
class VwapScheduleEngine;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;换成目录和命名空间以后，更自然的写法通常是：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-cpp&#34;&gt;namespace algo::twap {
class OrderManager;
class ScheduleEngine;
}

namespace algo::vwap {
class OrderManager;
class ScheduleEngine;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这一下，重复前缀没了，语义反而更清楚。因为“它属于哪个上下文”不再塞进类名里，而是交给目录和 &lt;code&gt;namespace&lt;/code&gt; 来表达。&lt;/p&gt;
&lt;p&gt;所以这套做法，本质上是在把&lt;strong&gt;上下文信息从名字里抽出去，交给结构来承载&lt;/strong&gt;。&lt;/p&gt;
&lt;h2 id=&#34;这背后更像按特性分包&#34;&gt;这背后更像“按特性分包”
&lt;/h2&gt;&lt;p&gt;如果你的目录是这样长的：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-text&#34;&gt;strategy/
  twap/
    order_manager.h
    schedule_engine.h
    slicer.h
  vwap/
    order_manager.h
    schedule_engine.h
    slicer.h
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;那你其实已经不是在做传统的“按技术层分包”了。&lt;/p&gt;
&lt;p&gt;传统分法更像这样：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-text&#34;&gt;models/
services/
utils/
controllers/
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这种分法前期看着整齐，后面业务一长，&lt;code&gt;service&lt;/code&gt; 目录就会像垃圾回收站。跟一个功能相关的类会被打散到不同目录里，读代码的时候脑子一直在跳。&lt;/p&gt;
&lt;p&gt;而 &lt;code&gt;twap&lt;/code&gt;、&lt;code&gt;vwap&lt;/code&gt; 这种分法，更接近 &lt;code&gt;package by feature&lt;/code&gt;。也就是一个目录先回答“这块业务是干什么的”，再在里面放这块业务自己需要的对象和流程。Java 世界里这个说法更常见，但放到 C++ 一样成立，只是承载它的不是原生 &lt;code&gt;package&lt;/code&gt;，而是“目录 + &lt;code&gt;namespace&lt;/code&gt; + 头文件边界”。&lt;/p&gt;
&lt;p&gt;说白了，你是在&lt;strong&gt;按变化原因组织代码，而不是按代码长相组织代码&lt;/strong&gt;。&lt;/p&gt;
&lt;h2 id=&#34;再往上走一步就是高内聚低耦合&#34;&gt;再往上走一步，就是高内聚低耦合
&lt;/h2&gt;&lt;p&gt;软件工程里老生常谈的那句“高内聚、低耦合”，其实就落在这种地方。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;twap&lt;/code&gt; 下面的对象彼此协作频繁，就应该放得近一点，命名空间也靠得近一点；&lt;code&gt;vwap&lt;/code&gt; 和它相似，但又不是一回事，就该给它单独边界。这样做有几个很直接的好处：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;同一个模块里的类名可以短一点&lt;/li&gt;
&lt;li&gt;模块之间的依赖更容易看出来&lt;/li&gt;
&lt;li&gt;重构时更容易判断改动会不会串到别的地方&lt;/li&gt;
&lt;li&gt;以后真要把某块能力拆成独立库，代价也小一些&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这也是为什么很多成熟项目最后都会长成“目录边界 + 命名空间边界 + 编译边界”尽量一致的样子。QuickFIX 这类项目里，你也能看到类似思路，类型和扩展点都尽量放在清晰的命名空间下面，而不是靠巨长的类名前缀去硬区分 &lt;a class=&#34;link&#34; href=&#34;https://quickfixengine.org/c/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;QuickFIX&lt;/a&gt; &lt;a class=&#34;link&#34; href=&#34;https://github.com/quickfix/quickfix&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;GitHub&lt;/a&gt;。&lt;/p&gt;
&lt;h2 id=&#34;什么时候它又不够了&#34;&gt;什么时候它又不够了
&lt;/h2&gt;&lt;p&gt;不过这事也别神化。&lt;/p&gt;
&lt;p&gt;文件夹分层再套 &lt;code&gt;namespace&lt;/code&gt;，只能说明你在往模块化方向走，不代表架构就自动好了。几个常见坑也很明显：&lt;/p&gt;
&lt;h3 id=&#34;目录和命名空间对不上&#34;&gt;目录和命名空间对不上
&lt;/h3&gt;&lt;p&gt;目录是 &lt;code&gt;twap/&lt;/code&gt;，代码里却还是一堆散着的全局类，或者 &lt;code&gt;namespace common&lt;/code&gt; 到处乱飞，这种基本等于白分。&lt;/p&gt;
&lt;h3 id=&#34;模块边界是假边界&#34;&gt;模块边界是假边界
&lt;/h3&gt;&lt;p&gt;表面上有 &lt;code&gt;twap&lt;/code&gt;、&lt;code&gt;vwap&lt;/code&gt;，实际上互相 &lt;code&gt;#include&lt;/code&gt;，公共逻辑随便穿透，最后只是把文件挪了位置，耦合一点没降。&lt;/p&gt;
&lt;h3 id=&#34;common-目录越长越胖&#34;&gt;&lt;code&gt;common&lt;/code&gt; 目录越长越胖
&lt;/h3&gt;&lt;p&gt;这个最常见。很多项目前面切得挺好，后面嫌麻烦，开始往 &lt;code&gt;common&lt;/code&gt;、&lt;code&gt;base&lt;/code&gt;、&lt;code&gt;util&lt;/code&gt; 里丢东西。丢到最后，真正稳定的抽象没沉淀下来，反而多了一个谁都能动、谁都依赖的大坑。&lt;/p&gt;
&lt;h3 id=&#34;结构只按层不按业务&#34;&gt;结构只按层，不按业务
&lt;/h3&gt;&lt;p&gt;如果你的目录永远是 &lt;code&gt;api&lt;/code&gt;、&lt;code&gt;service&lt;/code&gt;、&lt;code&gt;dao&lt;/code&gt;、&lt;code&gt;model&lt;/code&gt;，那很多业务概念其实没有家。类名只好越来越长，把本该由结构表达的东西全塞回名字里。&lt;/p&gt;
&lt;h2 id=&#34;那它到底该叫啥&#34;&gt;那它到底该叫啥
&lt;/h2&gt;&lt;p&gt;如果是在团队里沟通，我觉得可以分三档说法：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;只想说人话：&lt;strong&gt;按目录和命名空间做模块化&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;想说得更工程一点：&lt;strong&gt;按特性分包的组织方式，配合层级命名空间&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;如果这套边界已经和业务语义强绑定了：&lt;strong&gt;带 bounded context 味道的模块划分&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我自己更倾向第二种。因为它最贴近你描述的场景。&lt;/p&gt;
&lt;p&gt;你说的不是 C++ 语法技巧，也不是单纯命名规范，而是在做一件更实在的事：&lt;strong&gt;用结构承载语义，让名字回归本义&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;类名短下来，文件名短下来，读代码时先看到的是 &lt;code&gt;twap::OrderManager&lt;/code&gt; 这种上下文明确的对象，而不是 &lt;code&gt;TwapOrderManagerImpl&lt;/code&gt; 这种把目录职责、命名职责、实现职责全搅在一起的东西。&lt;/p&gt;
&lt;p&gt;这时候代码才有点像 Java 里包结构成熟之后那种感觉。&lt;/p&gt;
&lt;h2 id=&#34;最后一句&#34;&gt;最后一句
&lt;/h2&gt;&lt;p&gt;所以，软件工程上当然有对应概念，但不是只有一个标准答案。&lt;/p&gt;
&lt;p&gt;往大了说，它叫模块化。往代码组织上说，它叫按特性分包，再加命名空间管理。往领域边界上说，它又有点 bounded context 的意思。&lt;/p&gt;
&lt;p&gt;怎么说呢，这种实践最值钱的地方，从来不是“名字看起来整齐了”，而是你终于不用靠超长前缀去弥补结构缺位了。&lt;/p&gt;
&lt;h2 id=&#34;参考资料&#34;&gt;参考资料
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://en.cppreference.com/w/cpp/language/namespace&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;cppreference: Namespaces&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://quickfixengine.org/c/&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;QuickFIX/C++ 官方站点&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://github.com/quickfix/quickfix&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;QuickFIX GitHub 仓库&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://www.javapractices.com/home/HomeAction.do&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Java Practices: Package by feature, not layer&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;link&#34; href=&#34;https://martinfowler.com/bliki/BoundedContext.html&#34;  target=&#34;_blank&#34; rel=&#34;noopener&#34;
    &gt;Martin Fowler: Bounded Context&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;写作附记&#34;&gt;写作附记
&lt;/h2&gt;&lt;h3 id=&#34;原始提示词&#34;&gt;原始提示词
&lt;/h3&gt;&lt;blockquote&gt;
&lt;p&gt;提示词：在编码开发的时候，C++一种好的实践规范，那就是按照文件夹分层，然后每个类上面套个 namespace，类似 Java 的 package，能有效减少文件名称和类名字里面掺杂冗余的内容,有个经典的项目就是 quickfix，近期开发算法服务也用到了类似的设计，twap vwap 很多功能模块都是类似的，软件工程学上有没有专门的概念？&lt;/p&gt;&lt;/blockquote&gt;
&lt;h3 id=&#34;写作思路摘要&#34;&gt;写作思路摘要
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;从算法服务里 &lt;code&gt;twap&lt;/code&gt;、&lt;code&gt;vwap&lt;/code&gt; 的真实模块触发点切入，先把判断亮出来。&lt;/li&gt;
&lt;li&gt;没把问题硬解释成单一名词，而是拆成模块化、命名空间管理、按特性分包几个更准确的层次。&lt;/li&gt;
&lt;li&gt;保留了 QuickFIX 和 Java package 的对照关系，用来说明 C++ 经常要靠目录和命名空间补结构语义。&lt;/li&gt;
&lt;li&gt;重点落在“用结构承载语义”，而不是停留在“避免重名”这种表层解释。&lt;/li&gt;
&lt;li&gt;补了 &lt;code&gt;namespace&lt;/code&gt;、&lt;code&gt;package by feature&lt;/code&gt;、&lt;code&gt;bounded context&lt;/code&gt; 相关参考链接，方便后续继续展开。&lt;/li&gt;
&lt;/ul&gt;</description>
        </item>
        
    </channel>
</rss>
